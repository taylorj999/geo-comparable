var config = {};

// client configuration
// anything required by the presentation layer and not problematic for the user to see
config.client = {};

config.client.api_key = 'placeholder';

module.exports = config;